local options = {
  ensure_installed = { "lua", "javascript", "python", "c", "cpp" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
